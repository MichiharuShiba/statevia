import { ExecutionRunPage } from "@/features/executions/ui/ExecutionRunPage";

/**
 * 実行 Run ページ（薄い route wrapper）。
 */
export default function ExecutionRunRoutePage() {
  return <ExecutionRunPage />;
}
