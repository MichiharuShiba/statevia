import { ExecutionsPageClient } from "@/features/executions/ui/ExecutionsPageClient";

/**
 * 実行一覧ページ（薄い route wrapper）。
 */
export default function ExecutionsListPage() {
  return <ExecutionsPageClient />;
}
