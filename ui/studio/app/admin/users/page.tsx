import { AdminUsersPageClient } from "@/features/admin/ui/AdminUsersPageClient";

/**
 * テナント管理者向けユーザー管理画面（薄い route wrapper）。
 */
export default function AdminUsersPage() {
  return <AdminUsersPageClient />;
}
