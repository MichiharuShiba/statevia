import { AccountSettingsPageClient } from "@/features/auth/ui/AccountSettingsPageClient";

/**
 * ログイン中ユーザー向けアカウント設定（薄い route wrapper）。
 */
export default function AccountPage() {
  return <AccountSettingsPageClient />;
}
