import { DefinitionEditorPageClient } from "@/features/definition-editor/ui/DefinitionEditorPageClient";

/**
 * Definition 新規作成ページ（薄い route wrapper）。
 */
export default function DefinitionNewPage() {
  return <DefinitionEditorPageClient />;
}
