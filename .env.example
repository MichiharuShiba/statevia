# Docker Compose ローカル開発用。
# リポジトリルートで: cp .env.example .env
# .env はコミットしない（.gitignore）。

POSTGRES_USER=statevia
POSTGRES_PASSWORD=statevia
POSTGRES_DB=statevia
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
